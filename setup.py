from setuptools import setup

setup(
        name='yoctograd',
        version='0.1',
        description='A yocto-small autograd engine',
        author='Sebastian Hirt',
        packages=['yoctograd'],
        )
